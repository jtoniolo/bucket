{
  "name": "bucket",
  "version": "0.3.0",
  "description": "A native Claude Code plugin that runs an autonomous plan/execute/review/merge loop over a backlog of Tasks.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node build/bundle.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.19.21",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
