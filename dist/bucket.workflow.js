// GENERATED by build/bundle.mjs — do not edit.
// Source: src/workflow/bucket.workflow.js + inlined src/core (ADR-0005).
/**
 * Bucket Workflow — Slice 1 walking skeleton.
 *
 * The thinnest end-to-end path (ADR-0001): list ready Tasks from the Work
 * Source, take the single highest-priority one, run one implementer agent in an
 * isolated git worktree on a deterministic branch, gate on Commits-Ahead-of-Base,
 * merge that one branch into the Base Branch, and report the Task complete.
 *
 * No Plan dependency graph, no Review, no parallelism, no outer Loop yet — those
 * arrive in later slices. Deterministic decisions (branch name, the merge gate)
 * are computed here in JS from the inlined pure core; all shell/git/gh mutation
 * happens inside spawned agents, because the Workflow sandbox has no shell.
 *
 * Authoring note: this file is the bundle SOURCE. `build/bundle.mjs` prepends
 * the pure core as an IIFE assigned to `__bucketCore` and writes the
 * self-contained `dist/bucket.workflow.js` (ADR-0005). `meta`, `agent`,
 * `phase`, `log`, and `args` are provided by the Workflow runtime; the bundler
 * injects `__bucketCore`.
 */

export const meta = {
  name: "bucket",
  description:
    "Bucket walking skeleton: implement one ready Task in an isolated worktree and merge it into the base branch.",
  phases: [
    { title: "Plan", detail: "list ready Tasks and select the highest-priority one" },
    { title: "Execute", detail: "implement the Task on a deterministic branch in its own worktree" },
    { title: "Merge", detail: "gate on commits-ahead-of-base, then merge and close the Task" },
  ],
};

// --- inlined deterministic core (ADR-0005) ---
"use strict";
var __bucketCore = (() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // src/core/inline-entry.ts
  var inline_entry_exports = {};
  __export(inline_entry_exports, {
    branchFor: () => branchFor,
    commitsAheadOfBaseArgs: () => commitsAheadOfBaseArgs,
    fillCommand: () => fillCommand,
    parseTasks: () => parseTasks,
    selectHighestPriority: () => selectHighestPriority
  });

  // src/core/branch.ts
  function branchFor(task, format) {
    if (typeof format !== "string" || format.length === 0) {
      throw new Error("branchFor: branch format must be a non-empty string");
    }
    if (!format.includes("{id}")) {
      throw new Error(
        `branchFor: branch format must contain the "{id}" placeholder (got "${format}")`
      );
    }
    if (task == null || task.id === void 0 || task.id === null || task.id === "") {
      throw new Error("branchFor: task is missing an id");
    }
    return format.replaceAll("{id}", String(task.id));
  }

  // src/core/command.ts
  function fillCommand(template, vars = {}) {
    if (typeof template !== "string" || template.length === 0) {
      throw new Error("fillCommand: command template must be a non-empty string");
    }
    if (template.includes("{id}") && (vars.id === void 0 || vars.id === null)) {
      throw new Error(
        `fillCommand: template needs an "{id}" but none was provided (template: "${template}")`
      );
    }
    return template.replaceAll("{id}", vars.id === void 0 ? "" : String(vars.id));
  }

  // src/core/task.ts
  function parseTasks(json) {
    let raw;
    try {
      raw = JSON.parse(json);
    } catch (err) {
      throw new Error(
        `parseTasks: work source 'list' did not return valid JSON: ${err.message}`
      );
    }
    if (!Array.isArray(raw)) {
      throw new Error("parseTasks: work source 'list' must return a JSON array of Tasks");
    }
    return raw.map((entry, i) => {
      if (typeof entry !== "object" || entry === null) {
        throw new Error(`parseTasks: Task at index ${i} is not an object`);
      }
      const t = entry;
      if (typeof t.id !== "number") {
        throw new Error(`parseTasks: Task at index ${i} is missing a numeric "id"`);
      }
      if (typeof t.title !== "string") {
        throw new Error(`parseTasks: Task ${String(t.id)} is missing a string "title"`);
      }
      const labels = Array.isArray(t.labels) ? t.labels.filter((l) => typeof l === "string") : [];
      return { id: t.id, title: t.title, labels };
    });
  }
  function selectHighestPriority(tasks) {
    if (tasks.length === 0) return null;
    return tasks.reduce((best, t) => t.id < best.id ? t : best);
  }

  // src/core/git-commands.ts
  function commitsAheadOfBaseArgs(branch, base) {
    return ["rev-list", "--count", `${base}..${branch}`];
  }
  return __toCommonJS(inline_entry_exports);
})();
// --- end inlined core ---  (build/bundle.mjs replaces this marker with the inlined core)

// Pure deterministic core, inlined by the build step.
const { branchFor, fillCommand, parseTasks, selectHighestPriority, commitsAheadOfBaseArgs } = __bucketCore;

const TASK_LIST_SCHEMA = {
  type: "object",
  additionalProperties: false,
  required: ["stdout"],
  properties: {
    stdout: {
      type: "string",
      description: "The exact, unmodified stdout of the command (a JSON array of Tasks).",
    },
  },
};

const IMPLEMENT_SCHEMA = {
  type: "object",
  additionalProperties: false,
  required: ["committed"],
  properties: {
    committed: { type: "boolean", description: "Whether at least one commit was made on the branch." },
    summary: { type: "string", description: "One-line summary of what was done." },
  },
};

const COUNT_SCHEMA = {
  type: "object",
  additionalProperties: false,
  required: ["count"],
  properties: {
    count: { type: "integer", description: "Integer stdout of the git rev-list command." },
  },
};

const MERGE_SCHEMA = {
  type: "object",
  additionalProperties: false,
  required: ["merged"],
  properties: {
    merged: { type: "boolean", description: "Whether the branch was merged into the base branch." },
    closed: { type: "boolean", description: "Whether the Task was closed via the work source." },
    notes: { type: "string", description: "Any conflicts resolved or verification output worth noting." },
  },
};

const cfg = args;

// ── Plan ────────────────────────────────────────────────────────────────────
// The `list` command is the sole source of truth for ready work. Run it in an
// agent, then make the selection deterministically in JS.
phase("Plan");
const listResult = await agent(
  `Run exactly this shell command and return its stdout verbatim, with no commentary:\n\n` +
    `    ${cfg.workSource.list}\n\n` +
    `Do not modify, reformat, or summarise the output. Return the raw stdout.`,
  { label: "worksource:list", phase: "Plan", model: cfg.phases.plan.model, schema: TASK_LIST_SCHEMA },
);

const tasks = parseTasks(listResult.stdout);
const task = selectHighestPriority(tasks);

if (!task) {
  log("Plan: no ready Tasks — nothing to do.");
  return { status: "empty", reason: "no ready tasks" };
}

const branch = branchFor(task, cfg.branchFormat);
log(`Plan: selected Task #${task.id} "${task.title}" → branch ${branch}`);

// ── Execute ───────────────────────────────────────────────────────────────────
// One implementer agent works the Task in its own worktree on the deterministic
// branch. Resumption-ready: if the branch already exists, continue from it.
phase("Execute");
const viewCommand = fillCommand(cfg.workSource.view, { id: task.id });
const worktreePath = `.bucket/worktrees/issue-${task.id}`;
const testStep = cfg.test ? `Run the project's tests: \`${cfg.test}\`. ` : "";
const lintStep = cfg.lint ? `Run the project's linter: \`${cfg.lint}\`. ` : "";

const implement = await agent(
  [
    `You are Bucket's implementer for Task #${task.id}.`,
    ``,
    `1. Read the full Task body by running: \`${viewCommand}\` (and read any parent doc it references).`,
    `2. Set up an isolated git worktree on the deterministic branch \`${branch}\`, based on \`${cfg.baseBranch}\`:`,
    `   - If branch \`${branch}\` already exists, resume it: \`git worktree add ${worktreePath} ${branch}\`.`,
    `   - Otherwise create it: \`git worktree add -b ${branch} ${worktreePath} ${cfg.baseBranch}\`.`,
    `   Do all of your work inside ${worktreePath}.`,
    `3. Implement the Task test-first. ${testStep}${lintStep}`,
    `4. Commit your work. EVERY commit message MUST begin with the prefix "${cfg.commitPrefix}" followed by a space.`,
    ``,
    `Hard constraints (never violate): do NOT run \`git push\`, \`gh pr create\`, \`--no-verify\`,`,
    `force-push, or any history rewrite. Leave the branch and its worktree in place; do not delete them.`,
    `If you cannot finish, commit whatever compiles — partial progress is resumed next time.`,
  ].join("\n"),
  {
    label: `implement:issue-${task.id}`,
    phase: "Execute",
    model: cfg.phases.execute.model,
    effort: cfg.phases.execute.effort,
    schema: IMPLEMENT_SCHEMA,
  },
);
log(
  `Execute: implementer reported committed=${implement.committed}` +
    (implement.summary ? ` — ${implement.summary}` : ""),
);

// ── Merge ─────────────────────────────────────────────────────────────────────
// Trust the structure, not the agent's word: gate on Commits-Ahead-of-Base.
phase("Merge");
const gitCountCmd = `git ${commitsAheadOfBaseArgs(branch, cfg.baseBranch).join(" ")}`;
const gate = await agent(
  `Run exactly this command from the repository root and return its integer stdout:\n\n    ${gitCountCmd}\n`,
  { label: "gate:commits-ahead", phase: "Merge", model: cfg.phases.merge.model, schema: COUNT_SCHEMA },
);

if (gate.count <= 0) {
  log(`Merge: branch ${branch} has 0 commits ahead of ${cfg.baseBranch} — skipping (Task left open).`);
  return { status: "skipped", task: task.id, branch, reason: "zero commits ahead of base" };
}

log(`Merge: branch ${branch} is ${gate.count} commit(s) ahead — merging into ${cfg.baseBranch}.`);
const closeCommand = fillCommand(cfg.workSource.close, { id: task.id });
const merge = await agent(
  [
    `You are Bucket's merge step for Task #${task.id}.`,
    ``,
    `1. Check out \`${cfg.baseBranch}\` in the main working tree and merge branch \`${branch}\` into it,`,
    `   resolving any conflicts. Then verify the result${cfg.test ? ` by running \`${cfg.test}\`` : ""}.`,
    `2. Remove the Task's worktree: \`git worktree remove ${worktreePath}\` (use \`--force\` only if needed).`,
    `3. Report the Task complete via the work source: \`${closeCommand}\`.`,
    ``,
    `Hard constraints (never violate): do NOT run \`git push\`, \`gh pr create\`, \`--no-verify\`,`,
    `force-push, or any history rewrite. The merge stays local.`,
  ].join("\n"),
  {
    label: `merge:issue-${task.id}`,
    phase: "Merge",
    model: cfg.phases.merge.model,
    effort: cfg.phases.merge.effort,
    schema: MERGE_SCHEMA,
  },
);

log(`Merge: merged=${merge.merged} closed=${merge.closed} for Task #${task.id}.`);
return { status: "merged", task: task.id, branch, commitsAhead: gate.count, merge };
